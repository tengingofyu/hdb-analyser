{
  "name": "hdb-schools-parity-worker",
  "private": true,
  "version": "1.0.0",
  "description": "Cloudflare Worker proxying OneMap's School Query API for the HDB Analyser page",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^3.0.0"
  }
}
